import os from 'os';

// userInfo() [uid, gid, username, homedir, shell] or userInfo().[any form the list]
console.log(os.userInfo());

// totalmem()
console.log(os.totalmem());

// freemem()
console.log(os.freemem());

// cpus()
console.log(os.cpus());
